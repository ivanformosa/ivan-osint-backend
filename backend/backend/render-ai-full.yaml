services:
  - type: web
    name: ivan-osint-backend-ai-full
    env: python
    plan: standard
    rootDir: backend
    buildCommand: pip install -r requirements-ai-full.txt
    startCommand: gunicorn app:app --timeout 180
